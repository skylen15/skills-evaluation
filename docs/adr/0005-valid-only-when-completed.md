# Valid is true only on Completed

The sample Draft form shows Valid checked. Valid means the current official result for a member, so it is false until the CoE Head completes the Submission. Leaving it true from insert would show two official results during an in-progress cycle (the last Completed and the new Draft).
