# Keep application scope `x_711398_se`

The requirement ERD uses sample tables `x_63673_se_*`. This repo is already the scoped app `x_711398_se`. We keep that scope; runtime tables are `x_711398_se_submission` and siblings. Matching the screenshot prefix would mean recreating the app on the instance.
